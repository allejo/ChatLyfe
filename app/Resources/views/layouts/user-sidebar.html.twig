{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid py-2">
        <div class="row content">
            <aside class="col-sm-3 hidden-xs border-right">
                {% include '::navbar.html.twig' with { myChannels: myChannels | default([]) } %}
            </aside>

            <div class="col-sm-9">
                {% block layout_body %}
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}
