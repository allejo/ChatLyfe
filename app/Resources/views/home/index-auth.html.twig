{% extends 'base.html.twig' %}

{% block title %}
    ChatLyfe - Enjoy your chat with everyone
{% endblock %}

{% block body %}
    <div class="container-fluid py-2">
        <div class="row content">
            <aside class="col-sm-3 hidden-xs">
                {% include '::navbar.html.twig' with { myChannels: myChannels } %}
            </aside>

            <div class="col-sm-9">
                <div class="row">
                    {% for channel in allChannels %}
                        <div class="col-sm-3">
                            <a href="{{ path('view_chat', { id: channel.id }) }}"
                               class="btn btn-primary--chatlyfe text-center mb-3 py-3 d-block"
                            >
                                <p class="m-0"><strong>{{ channel.name }}</strong></p>
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
